import React from 'react';
import { Layout } from 'components';

const ContactPage = () => (
    <Layout centered>
        <h1>Contact Page</h1>
    </Layout>
);

export default ContactPage;
