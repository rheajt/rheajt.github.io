# Domain docs

This is a single-context repository.

Before exploring a domain area, read the root `CONTEXT.md` when it exists and
the relevant records in `docs/adr/`. These files are created lazily when the
domain model or a durable architectural decision needs to be recorded.
